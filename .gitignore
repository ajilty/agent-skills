# Machine / control state (ADR-0005: two-axis split — tracked docs/ vs gitignored .agents/)
# Exception: .agents/skills/ holds vendored dev skills (installed via `npx skills`), tracked.
.agents/*
!.agents/skills/

# Harness-managed worktrees / local state
.claude/worktrees/
